DEFAULT_EMAIL=
NGINX_PROXY_CONTAINER=userver-nginx-proxy
# Leave NGINX_DOCKER_GEN_CONTAINER unset for nginxproxy/nginx-proxy (combined image). Only set it
# when docker-gen runs in a different container than nginx.
